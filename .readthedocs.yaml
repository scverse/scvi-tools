version: 2
build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
sphinx:
  configuration: docs/conf.py
python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docsbuild
submodules:
  include:
    - "docs/tutorials/notebooks"
  recursive: true
